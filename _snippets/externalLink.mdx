<ResponseField name="platform_id" type="string" required>
  The accounting platform's unique identifier for the entity
</ResponseField>

<ResponseField name="data_model" type="string" required>
  The data model to which this link is related.
</ResponseField>

<ResponseField name="data_model_id" type="string" required>
  The unique identifier for the data model associated with this link
</ResponseField>

<ResponseField name="link" type="string" required>
  The URL or URI that provides direct access to the associated data model or document.
</ResponseField>

<ResponseField name="type" type="enum" optional>
  The type of link. Example values can be: WEBSITE, EMAIL, etc.
</ResponseField>

<ResponseField name="updated_at" type="date" required>
  The date and time at which the link information was last updated in the accounting platform
</ResponseField>
