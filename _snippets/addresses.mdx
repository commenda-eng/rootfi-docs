<ResponseField name="platform_id" type="string" required>
  The accounting platform's unique identifier for the address
</ResponseField>

<ResponseField name="type" type="enum" required>
  The type of address. Values can be: BILLING, SHIPPING, etc.
</ResponseField>

<ResponseField name="data_model" type="string" required>
  The data model to which this address is linked. Example values can be: CONTACT, INVOICE, ORDER, etc.
</ResponseField>

<ResponseField name="data_model_id" type="string" required>
  The unique identifier for the data model associated with this address
</ResponseField>

<ResponseField name="street" type="string" optional>
  The street information of the address
</ResponseField>

<ResponseField name="locality" type="string" optional>
  The locality or neighborhood information of the address
</ResponseField>

<ResponseField name="city" type="string" required>
  The city information of the address
</ResponseField>

<ResponseField name="state" type="string" required>
  The state or region information of the address
</ResponseField>

<ResponseField name="country" type="string" required>
  The country information of the address
</ResponseField>

<ResponseField name="pincode" type="string" optional>
  The postal or zip code of the address
</ResponseField>

<ResponseField name="updated_at" type="date" required>
  The date and time at which the address information was last updated in the accounting platform
</ResponseField>
