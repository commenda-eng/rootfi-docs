<ResponseField name="platform_id" type="string" required>
  The accounting platform's ID for this currency
</ResponseField>

<ResponseField name="name" type="string">
  The name of the currency
</ResponseField>

<ResponseField name="code" type="string">
  The international standard code for this currency
</ResponseField>

<ResponseField name="symbol" type="string">
  The symbol used to represent this currency
</ResponseField>

<ResponseField name="is_base_currency" type="boolean">
  Indicates whether this is the base currency for the accounting platform
</ResponseField>

<ResponseField name="exchange_rate" type="float">
  The current exchange rate of this currency against the base currency
</ResponseField>

<ResponseField name="updated_at" type="date" required>
  The date and time at which the currency information was last updated
</ResponseField>

<ResponseField name="raw_data" type="object">
  The raw JSON response we get from the accounting platform for that data.
</ResponseField>
