---
openapi: unified-api put /company/{company_id}
---

<RequestExample>

```bash cURL

curl --request PUT \
  --url https://integrate.rootfi.dev/company/{company_id} \
  --header 'Content-Type: application/json' \
  --header 'api_key: <api_key>' \
  --data '{
  "name": "RootFi Test Company"
}'
```

```javascript NodeJS
import { RootFiClient } from "rootfi-api";

const rootfi = new RootFiClient({
  apiKey: "<api_key>",
});

await rootfi.core.companies.update(123456, {
  name: "Test",
});
```

</RequestExample>
