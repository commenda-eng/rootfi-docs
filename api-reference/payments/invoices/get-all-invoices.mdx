---
openapi: openapi get /payments/invoices
---

[The Invoices Data Model](https://docs.rootfi.dev/api-reference/invoices/overview) is designed to streamline the management and retrieval of invoices information across various payments platforms.

<RequestExample>

```bash cURL
curl --request POST \
  --url https://api.rootfi.dev/v3/payments/invoices  \
  --header 'Content-Type: application/json' \
  --header 'api_key: <api_key>' \
  --data '{
  "take": 10,
  "skip": 0,
  "orderBy": {
    "rootfi_created_at": "asc"
  },
  "filter": {
    "status": {
      "eq": "PAID"
    }
  }
}'
```

```javascript NodeJS
import { RootFiClient } from "rootfi-api";
const rootfi = new RootFiClient({ apiKey: "<api_key>" });

await rootfi.payments.invoices.list({
  take: 10,
  skip: 0,
  filter: {
    status: { eq: "PAID" },
  },
  orderBy: {
    rootfi_id: "asc",
  },
});
```

</RequestExample>
