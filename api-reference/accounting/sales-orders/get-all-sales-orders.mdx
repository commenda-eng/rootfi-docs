---
openapi: openapi post /accounting/sales_orders/all
---

A **Sales order** is a record of goods or services sold to a customer

<RequestExample>

```bash cURL
curl --request POST \
  --url https://api.rootfi.dev/accounting/sales_orders/all \
  --header 'Content-Type: application/json' \
  --header 'api_key: <api-key>' \
  --data '{
  "take": 10,
  "skip": 0,
  "orderBy": {
    "total_amount": "asc"
  },
  "filter": {
    "status": {
      "eq": "SUBMITTED"
    }
  }
}'
```

```javascript NodeJS
import { RootFiClient } from "rootfi-api";

const rootfi = new RootFiClient({
  apiKey: "<api_key>",
});

await rootfi.accounting.salesOrder.list({
  take: 10,
  skip: 0,
  orderBy: {
    total_amount: "asc",
  },
  filter: {
    status: { eq: "SUBMITTED" },
  },
});
```

</RequestExample>
