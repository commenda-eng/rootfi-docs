---
version: "v2"
openapi: openapi_v2 get /integrations
---

<RequestExample>

```bash cURL
curl --request GET \
  --url https://api.rootfi.dev/integrations \
  --header 'api_key: <api-key>'
  --data '{"type": "ALL"}'
```

```javascript NodeJS
import { RootFiClient } from "rootfi-api";
const rootfi = new RootFiClient({ apiKey: "<api_key>" });

await rootfi.core.integrations.list({ type: "ALL" });
```

</RequestExample>
