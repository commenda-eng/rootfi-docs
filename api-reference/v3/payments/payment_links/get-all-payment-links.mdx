---
version: "v3"
openapi: openapi_v3 get /payments/payment_links
---

[The Orders Data Model](https://docs.rootfi.dev/api-reference/payment_links/overview) is designed to streamline the management and retrieval of payment_links information across various payments platforms.

<RequestExample>

```bash cURL
curl --request POST \
  --url https://api.rootfi.dev/v3/payments/payment_links  \
  --header 'Content-Type: application/json' \
  --header 'api_key: <api_key>' \
  --data '{
  "take": 10,
  "skip": 0,
  "orderBy": {
    "rootfi_created_at": "asc"
  },
  "filter": {
    "status": {
      "eq": "ACTIVE"
    }
  }
}'
```

```javascript NodeJS
import { RootFiClient } from "rootfi-api";
const rootfi = new RootFiClient({ apiKey: "<api_key>" });

await rootfi.payments.payment_links.list({
  take: 10,
  skip: 0,
  filter: {
    status: { eq: "ACTIVE" },
  },
  orderBy: {
    rootfi_id: "asc",
  },
});
```

</RequestExample>
