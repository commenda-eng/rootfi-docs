---
version: "v3"
openapi: openapi_v3 get /payments/tax_rates
---

[The Tax Rates Data Model](https://docs.rootfi.dev/api-reference/payments/overview) is designed to streamline the management and retrieval of tax_rates information across various payment platforms.

<RequestExample>

```bash cURL
curl --request POST \
  --url https://api.rootfi.dev/v3/payments/tax_rates  \
  --header 'Content-Type: application/json' \
  --header 'api_key: <api_key>' \
  --data '{
  "take": 10,
  "skip": 0,
  "orderBy": {
    "rootfi_created_at": "asc"
  },
  "filter": {
    "name": {
      "eq": "TAX01"
    }
  }
}'
```

```javascript NodeJS
import { RootFiClient } from "rootfi-api";
const rootfi = new RootFiClient({ apiKey: "<api_key>" });

await rootfi.payments.tax_rates.list({
  take: 10,
  skip: 0,
  filter: {
    name: { eq: "TAX01" },
  },
  orderBy: {
    rootfi_id: "asc",
  },
});
```

</RequestExample>
