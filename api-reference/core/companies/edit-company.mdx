---
openapi: openapi put /core/companies/{company_id}
---

<RequestExample>

```bash cURL

curl --request PUT \
  --url https://integrate.rootfi.dev/v3/core/companies/{company_id} \
  --header 'Content-Type: application/json' \
  --header 'api_key: <api_key>' \
  --data '{
  "name": "RootFi Test Company"
}'
```

```javascript NodeJS
import { RootFiClient } from "rootfi-api";
const rootfi = new RootFiClient({ apiKey: "<api_key>" });

await rootfi.core.companies.update(1234, {
  name: "RootFi Test Company",
});
```

</RequestExample>
