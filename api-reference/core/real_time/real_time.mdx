---
openapi: openapi post /core/real_time
---

<RequestExample>

```bash cURL
curl --request POST \
  --url https://api.rootfi.dev/v3/core/real_time \
  --header 'Content-Type: application/json' \
  --header 'api_key: <api-key>' \
  --data '{
  "company_id": 1,
  "data_model": "COMPANY_INFO",
  "variables": {
    "per_page": 100,
    "page": 1
  }
}'
```

```javascript NodeJS
import { RootFiClient } from "rootfi-api";
const rootfi = new RootFiClient({ apiKey: "<api_key>" });

await rootfi.core.real_time.list({
  company_id: 1,
  data: {
    company_id: 1,
    data_model: "COMPANY_INFO",
    variables: {
      per_page: 100,
      page: 1,
    },
  },
});
```

</RequestExample>
